import DashboardClient from "./DashboardClient";

export const metadata = {
  title: "Ultimate Salary Dashboard | Multi-Utility Hub",
  description: "Analyze your entire salary structure, unlock hidden tax savings, and project your retirement corpus in one place.",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
